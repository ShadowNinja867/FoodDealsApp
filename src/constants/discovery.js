/** When adding a deal: search OpenStreetMap inside this radius around the user. */
export const BRANCH_SEARCH_RADIUS_MILES = 1;

/** Map pins: only saved deals within this distance of the user (when location is known). */
export const VISIBLE_DEALS_RADIUS_MILES = 5;
